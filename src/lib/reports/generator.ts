// DEPRECATED: Reescrever usando Supabase
// import { prisma } from '@/lib/prisma'

// import { createClientComponentClient } from '@supabase/auth-helpers-nextjs'

export async function generateExecutiveReport(type: any, params: any) {
    // const supabase = createClientComponentClient()

    // TODO: Reimplementar com Supabase
    throw new Error('Report generator em migração para Supabase')
}

// ... resto das funções com mesmo padrão
